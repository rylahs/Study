#include <bits/stdc++.h>
using namespace std;

int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int testCase;
	cin >> testCase;

	while (testCase--)
	{
		int N, M;
		cin >> N >> M;

		queue<int> q;
		for (int i = 0; i < N; i++)
		{
			int input;
			cin >> input;
			q.push()
		}
	}


	return 0;
}