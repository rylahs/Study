#include <bits/stdc++.h>
using namespace std;
int main_2742(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    while (n != 0)
    {
        cout << n << '\n';
        n--;
    }
    return 0;
}