#include <bits/stdc++.h>
using namespace std;

int main_10951(void)
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	while (1)
	{
		int a, b;
		if ((cin >> a >> b).eof() == true)
			break;
		
		cout << a + b << '\n';
	}

	return 0;
}