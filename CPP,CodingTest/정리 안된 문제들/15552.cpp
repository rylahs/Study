#include <bits/stdc++.h>
using namespace std;

int main_15552(void)
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int testCase;
	cin >> testCase;
	for (int t = 0; t < testCase; t++)
	{
		int a, b;
		cin >> a >> b;
		cout << a + b << "\n";
	}
	return 0;
}